# Local dev: a plain file-backed SQLite database via libSQL
LIBSQL_URL=file:./data/tracker.db

# Production (Turso): uncomment and set these in Vercel's project env vars instead
# LIBSQL_URL=libsql://<your-db-name>-<your-org>.turso.io
# LIBSQL_AUTH_TOKEN=<your-turso-auth-token>

PORT=4000
